<%= form_with(model: brunch, local: true) do |form| %>
  <% if brunch.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(brunch.errors.count, "error") %> prohibited this brunch from being saved:</h2>

      <ul>
      <% brunch.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :city %>
    <%= form.text_field :city %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
